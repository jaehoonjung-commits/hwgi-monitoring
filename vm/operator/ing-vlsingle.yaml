apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
  name: vlsigle
  namespace: monitoring
spec:
  ingressClassName: traefik
  rules:
  - host: vl-single.node-02
    http:
      paths:
      - backend:
          service:
            name: vlsingle-vl-single
            port:
              number: 8429
        path: /
        pathType: Prefix
